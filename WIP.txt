module.exports = {
    name: '',
    description: '',
    execute(message, args) {
        message.channel.send(`${message.author}` + " `` is not ready for use yet!")
    }
}

else
        {
            message.channel.send(`${message.author} the sub-commands for \`\` have not been implemented yet.`)
        }