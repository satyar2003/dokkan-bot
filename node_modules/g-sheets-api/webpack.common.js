const path = require("path");

const { CleanWebpackPlugin } = require('g-sheets-api/node_modules/clean-webpack-plugin');
const HtmlWebpackPlugin = require('g-sheets-api/node_modules/html-webpack-plugin');


module.exports = {
    entry: {
        app: './demo/index.js'
    },
    plugins: [
        new CleanWebpackPlugin(),
        new HtmlWebpackPlugin({
            title: 'Production',
            inject: 'footer',
            template: './demo/index.html'
        })
    ],
    output: {
        filename: '[name].bundle.min.js',
        path: path.resolve(__dirname, "app")
    },
    module: {
        rules: [
            {
                test: /\.js$/,
                exclude: /(node_modules)/,
                use: {
                    loader: 'babel-loader',
                    options: {
                        cacheDirectory: true
                    }
                }
            }
        ]
    }
};
